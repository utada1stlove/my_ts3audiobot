# TS3AudioBot 自动安装脚本计划

- 状态：完成
- 目标：新增可重复使用的 Linux 安装脚本，安装固定的 ArthurZhu1992/TS3AudioBot `v0.1.0`，并说明其使用方法。
- 交付物：`scripts/install-ts3audiobot.sh`；`INSTALL_GUIDE_CN.md` 中的自动化安装章节。
- 安全约束：仅 root 可执行；固定版本与 SHA-256；默认绑定 Web 到 `127.0.0.1`；不改防火墙；拒绝覆盖已有安装；支持经 `--archive` 使用本地已下载包。
- 验收：`bash -n` 通过；帮助文本可运行；脚本中版本、下载 URL、哈希和服务配置经静态检查；工作区验证通过。
- 清理：不生成安装包或编译中间文件。

## 完成记录

1. 已新增固定 `v0.1.0` 的 Debian/Ubuntu 自动安装脚本和本地使用说明。
2. 脚本支持 `x86_64` 与 `aarch64`，分别固定官方 SHA-256；可直接下载，或经 `--archive` 对本地 ZIP 校验后安装。
3. 脚本拒绝覆盖现有安装、拒绝非预期或含 `..` 路径段的 ZIP 条目，并默认将 Web 服务绑定到 `127.0.0.1`。
4. 已通过 `bash -n`、`--help`、静态约束检查和 `basic-validation` 工作区验证；未生成构建中间文件。
